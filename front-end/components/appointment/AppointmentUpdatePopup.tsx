interface Props {
    appointmentId: number;
}

/**
 * this is only if you have time left. this is a popup where you can choose which admin to add to an appointment
 * which is part of the requirement PUT
 * if you have an easier way just delete this file and put the logic in the index
 * @param appointmentId
 * @constructor
 */
const AppointmentUpdatePopup: React.FC<Props> = ({appointmentId}) => {

    return (

    )
}

export default AppointmentUpdatePopup;

